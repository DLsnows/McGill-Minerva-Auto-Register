# Revisions whose changes are pure formatting — exclude them from `git blame`.
#
# Usage (opt in per clone; GitHub also honours this file for blame on the web UI):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add one line per bulk-format commit, newest first.
#
# NOTE: hashes change if this branch is squashed or rebased before merging — re-point the
# entry at the resulting commit then. A hash that no longer resolves is ignored by git
# (blame just shows the formatting commit), so a stale entry is harmless, only useless.

# docs/plans: reformatted so the directory could join the formatting gate (whitespace only)
baac2b2cb79afb46a493b28f1a92cfc8116f7ed4
